const GITHUB = import.meta.env.VITE_GITHUB;
const STACKOVERFLOW = import.meta.env.VITE_STACKOVERFLOW;

const URL_SHORTNER = import.meta.env.VITE_URL_SHORTNER;
const User_Management = import.meta.env.VITE_User_Management;
const NETFLIX_CLONE = import.meta.env.VITE_NETFLIX_CLONE;
const ECOMMERCE = import.meta.env.VITE_ECOMMERCE;
const IMAGE_EDITOR = import.meta.env.VITE_IMAGE_EDITOR;
const QUIZ_APP = import.meta.env.VITE_QUIZ_APP;

export {
  STACKOVERFLOW,
  GITHUB,
  URL_SHORTNER,
  User_Management,
  NETFLIX_CLONE,
  ECOMMERCE,
  IMAGE_EDITOR,
  QUIZ_APP,
};
